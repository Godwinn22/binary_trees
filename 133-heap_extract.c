#include "binary_trees.h"

/**
 * heap_extract - function that extracts the root node of
 * a Max Binary Heap
 * @root: the double pointer to the root node of heap
 *
 * Return:returns the value stored in the root node,0 on failure
 */
int heap_extract(heap_t **root)
{

}
